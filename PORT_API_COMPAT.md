# API Compatibility Audit — Rust Backend vs. travellermap.com `/doc/api`

> Generated 2026-06-12 (background agent). Source of truth: `doc/api.html` (local copy of
> https://travellermap.com/doc/api), cross-checked against `server/api/*.cs`. Our backend:
> `crates/backend/src/main.rs`, `crates/backend/src/search.rs`; wire types in `crates/core/src/dto.rs`.

## Headline (2026-06-16) — the documented data API is implemented and live-parity-tested

**Decision taken:** adopt the public PascalCase shapes + documented URLs + `/data/...` aliases as the single
contract (not a parallel private one). **Every documented *data* endpoint is now implemented** — coordinates,
universe, milieux, search (full query language), sec (Tab/SecondSurvey/legacy SEC), msec, metadata, credits,
jumpworlds, route, t5ss/{allegiances,sophonts} — with content negotiation (**JSONP** + **XML**, where the format
applies) and the semantic `/data/{sector}/...` URL family (incl. subsector/quadrant region aliases and the bare
`/data/{sector}/{hex}` single-world lookup). `POST /api/sec` and `POST /api/metadata` (reformat / convert) are
wired. The `compat_suite` drives the real router in-process and checks **golden fixtures + live travellermap.com
parity** for each; **0 ignored tests** remain.

The image endpoints (Tile/Poster/JumpMap) are intentionally **N/A** — those render client-side in this rewrite.
Our private streaming extensions (`/api/sector/{milieu}/{name}?lod=`, `/api/overlays`) coexist as additive
endpoints with no public equivalent. Remaining gaps are minor/niche — see **"Remaining gaps"** at the bottom.

## Compatibility matrix

| Endpoint (URL pattern) | Purpose | Reference handler | Data / Render | Our status | Gap / what's needed |
|---|---|---|---|---|---|
| `GET /api/universe` • `GET /data` | List all sectors (X,Y, names, abbr, tags) for a milieu | `UniverseHandler.cs` | Data | **DONE** | **Unified + complete:** `get_universe` emits `{"Sectors":[{"X","Y","Milieu","Abbreviation","Tags","Names":[{Text,Lang?,Source?}]}]}` and our Leptos client consumes it. `load_universe` now aggregates **all** `milieu.tab` metafiles (port of `SectorMap`), assigning `CanonicalMilieu` (the `<Sector Milieu>` attr, else `M1105`), keeping **positioned-but-dataless** sectors, merging each sector's per-sector `MetadataFile` name list (`Sector.Merge`), and deduping combined tags (reference `OrderedHashSet`). Filters: `requireData`, `tag` (repeatable/comma), `era` (milieu alias). **1021/1021 M1105 sectors, count-parity with live.** Field-exact vs live except a few drift-only abbreviations (live has hand-disambiguated `Inc2`/`Inc3` this checkout lacks). Remaining: XML output. |
| `GET /api/milieux` | List milieux (`Code`,`IsDefault`) | `UniverseHandler.cs` (`MilieuxCodesHandler`) | Data | **DONE** | `compat::get_milieux` — canonical milieu codes (`M<n>`/`IW`) in `milieu.tab` order, byte-for-byte vs live `[{"Code","IsDefault"}]`. +JSONP. |
| `GET /api/search?q=` | Name/attribute search → worlds/subsectors/sectors/labels | `SearchHandler.cs` | Data | **DONE (2026-06-16)** | `get_search` emits the canonical `{"Results":{"Count","Items":[{"World"\|"Sector"\|"Subsector"\|"Label":{…}}]}}` (externally-tagged `SearchItem`). **Full documented query language ported** (`crates/core/src/searchlang.rs`): `*`/`%`/`?`/`_`/`[…]` wildcards via a hand-rolled SQL-Server-`LIKE` matcher, `exact:`, `like:` (hand-rolled SOUNDEX), scopes `uwp: pbg: zone: alleg: ix: ex: cx: stellar: remark: in:`, bare `sector`/`subsector`/`world` type keywords, multi-word AND, the `^\w{7}-\w$` UWP shortcut, the `sector NNNN` sector+hex shortcut, and `types=`. **Now backed by an embedded Tantivy index** (`tantivy = "0.22"`, `Index::create_in_ram`, built per-milieu at first use): each clause maps onto a `RegexQuery` (LIKE→regex via `tmap_core::searchlang::like_to_regex`, verified behaviorally identical to `like_match`) or an exact `TermQuery` over **raw** (untokenized, lowercased) fields — *not* tokenized BM25 — so semantics are byte-identical to the old scan; ranking by `{Ix}` importance stays in Rust. Index carries per-world fields + subsectors + aggregated **labels** (border/`<Label>` text). `NUM_RESULTS=160`, importance-ranked. Verified vs `refs/search_regina.json` + live for the doc examples (`r*a`, `re*in`/`re*in*`, `exact:sol`, `like:tear`, `so ri`, `uwp:`, `in:`, UWP shortcut), **plus 12 per-query live-parity tests** (`parity_search_*`) that diff the result list **in order** against live for distinct queries across every scope/shortcut — i.e. full ranking parity, not just set membership. Two real divergences found+fixed by those tests (2026-06-17): (a) all-caps high-population world names are now title-cased (`SectorParser` `FixCapitalization`, e.g. `TERRA`→`Terra`); (b) borders with no explicit `Label` are now labeled by their **allegiance name** (`Border.GetLabel` fallback via the sector's `<Allegiance>` table), which a region's averaged search position depends on. **XML + JSONP done.** **Ranking matches in order**, including the within-importance tie-break: results sort by `{Ix}` importance descending, ties broken by each kind's reference SQL `SELECT DISTINCT` coordinate-column order (worlds `(sector_x, sector_y, hex_x, hex_y)`, etc.) — all 12 per-query tests pass an ordered diff vs live. **Remaining (niche):** only the `(random world)` + canned `(name)`→`res/search/*.json` specials. |
| `GET /api/coordinates?sector=&hex=` (+`subsector`,`sx/sy`,`hx/hy`,`x/y`) • `/data/{sector}/coordinates` • `/data/{sector}/{hex}/coordinates` | Sector lookup + coord conversion → `sx,sy,hx,hy,x,y` | `CoordinatesHandler.cs` | Data | **DONE** (`/data/...` aliases pending) | `compat::get_coordinates` — all param combos (sector by name/abbrev + hex/subsector, `sx/sy[+hx/hy]`, `x/y`) via `tmap_core::astrometrics::{location_to_coordinates,coordinates_to_location}`. Byte-for-byte vs live, +JSONP, +XML, +`/data/{sector}[/{hex}]/coordinates` aliases. **Complete.** |
| `GET /api/jumpworlds?…` • `…/jump/{jump}` | Worlds within N parsecs (0–12, default 6) | `JumpWorldsHandler.cs` | Data | **DONE** (JSON) | `get_jumpworlds` ports `HexSelector`: loads candidate sectors over the ±(jump+1) absolute-coord bbox into a coord→world map, then **raster-scans (y,x)** filtering by `astrometrics::reference_hex_distance` (a verbatim `Astrometrics.HexDistance` port — the column-parity correction `Coord::hex_distance` deliberately omits). Emits the full public `WorldResult` (SS/Subsector/Quadrant/WorldX/Y/ResourceUnits/LegacyBaseCode via `encode_legacy_bases`/SubsectorName/SectorAbbreviation/AllegianceName). Verified exact vs live for SM 1910 j2 (11 worlds, exact order). `/data/{sector}/{hex}/jump/{jump}` and the bare **`/data/{sector}/{hex}`** (jump=0 → single world; `data_world`, 2026-06-16, worldgen's lookup; verified byte-identical to live for Regina/Noricum/Deneb) are both wired. **XML + JSONP done. Complete.** |
| `GET /api/sec?sector=` (+`subsector`/`quadrant`/`sx/sy`) • `POST /api/sec` • `/data/{sector}` `/sec` `/tab` … | Sector UWP data as SEC / SecondSurvey / TabDelimited text | `SECHandler.cs` | Data | **DONE** (TabDelimited + SecondSurvey) | `get_sec` serves `type=TabDelimited` and `type=SecondSurvey` text (ported `SectorWriter` → `tmap_core::sector_writer`: TabDelimited + SecondSurvey + `ColumnSerializer`), with `subsector`/`quadrant`/`sx,sy` selection, `metadata`/`header`/`sscoords` toggles, the SEC/SecondSurvey `# …` metadata block, and synthesized abbreviations. Verified byte-for-byte vs live across many sectors. **Now also: legacy fixed-column `SEC`** (`type=SEC`, via `write_legacy_sec` + T5→legacy allegiance/base encoders; the no-`type` default is **SecondSurvey**, matching live), `/data/{sector}[/sec|/tab]` + subsector/quadrant region aliases, and **`POST /api/sec`** reformat. **Complete.** |
| `GET /api/metadata?sector=` (+`sx/sy`) • `POST` • `/data/{sector}/metadata` | Sector metadata (allegiances/borders/routes/labels/subsectors) XML/JSON | `SectorMetaDataHandler.cs` | Data | **DONE** (JSON) | `get_metadata` emits the full documented JSON (`tmap_core::metadata`: Selected/Tags/Abbreviation/Names/Credits/X/Y/Products/DataFile/Subsectors/Allegiances/Stylesheet/Labels/Borders/Regions/Routes). Parses the sector `.xml` **once** (vs the reference's per-field re-parse), computed border `LabelPosition`, worlds+borders-derived `Allegiances` with `Base`. Verified exact vs live for Spinward Marches (routes compared by resolved absolute coords). **Now also: XML output** (`SectorMetadata::to_xml`, `[XmlRoot("Sector")]` shape — 171 elements, per-collection counts match live), **JSONP**, **`POST /api/metadata`** (XML→JSON convert), and the `/data/{sector}/metadata` alias. **Complete.** Documented equivalences vs live (already normalized in the JSON parity test): allegiance ordering, the `Route.FixHex` start/end notation, and named-color casing (`olivedrab` vs `OliveDrab`). |
| `GET /api/msec?sector=` • `/data/{sector}/msec` | MSEC (sec2pdf) metadata text | `MSECHandler.cs` | Data | **DONE (2026-06-16)** | `MSECWriter.cs` ported to `crates/core/src/msec_writer.rs` (sector/domain/quadrant header, `a`–`p` subsectors, per-allegiance `ally`/`route`/`label`/`border` groups; `Route.FixHex` out-of-sector normalization; stylesheet color cascade over `res/styles/otu.css`). `get_msec` + `/api/msec` + `/data/{sector}/msec`. Within-group line order is normalized in the parity test (the reference uses .NET's unstable `List.Sort`, not byte-reproducible); header/groups/line-text byte-exact. Verified vs `refs/msec_sm.msec` + live. Remaining: XML/JSONP. |
| `GET /api/credits?sector=&hex=` (+`sx/sy/hx/hy`,`x/y`) • `/data/{sector}/credits` • `…/{hex}/credits` | Per-location world data + sector credits/author/source | `CreditsHandler.cs` | Data | **DONE** (JSON) | `get_credits` reuses the shared `assemble_metadata` parse (sector `.xml` over inline region block) for Credits/Author/Source/Publisher/Copyright/Tags/Products + subsector (by hex→letter) + world lookup at the hex. Hex defaults to 1620 (`SectorCentralHex`). Null fields omitted, matching the reference serializer. Verified exact vs live for Spinward Marches 1910. **XML + JSONP + `/data/{sector}[/{hex}]/credits` aliases done. Complete.** (`RouteCredits`/`SubsectorCredits`/`WorldCredits` are always empty in current data.) |
| `GET /api/route?start=&end=&jump=` (+`wild`,`im`,`nored`,`aok`,`x/y`,`milieu`) | Shortest jump route between two worlds | `RouteHandler.cs` | Data | **DONE** (JSON) | `get_route` over the cross-sector route index (constrained pathfinder: refuel/Imperial/red-zone/anomaly). Default emits the documented public **bare array of `RouteStop`** (Sector/SectorX/SectorY/**Subsector**/Name/Hex/HexX/HexY/UWP/PBG/Zone/AllegianceName). **Now also: per-stop `Subsector` name** (resolved at index build), **XML** (`<ArrayOfRouteStop>`) + **JSONP**, and a **coord-decode fix** — `SectorX/Y`+`HexX/Y` are recovered with the inverse of the pathfinder's naive `sector*32+col` packing (previously decoded via Astrometrics → wrong `SectorY`/`HexX`); `route_public_shape` now asserts **full** coord parity vs live. Private `&detail=true` returns the rich `{waypoints,jumps,parsecs}` the Leptos client draws from. **Complete.** |
| `GET /t5ss/allegiances` | Allegiance code→name | `CodesHandler.cs` | Data | **DONE** | `compat::get_allegiances` — `res/t5ss/allegiance_codes.tab` → `[{"Code","LegacyCode","Name","Location"}]`, sorted by code. +JSONP. |
| `GET /t5ss/sophonts` | Sophont code→name | `CodesHandler.cs` | Data | **DONE** | `compat::get_sophonts` — `res/t5ss/sophont_codes.tab` → `[{"Code","Name","Location"}]`, sorted by code. +JSONP. |
| `GET /api/tile?x=&y=&scale=` | Render rectangle → PNG/JPEG/PDF/SVG | `TileHandler.cs` | **Render** | **N/A (client-render)** | Intentionally not served; client renders from data streams. |
| `GET /api/poster?…` • `POST` • `/data/{sector}[/{sub}]/image` | Render poster → image/PDF/SVG | `PosterHandler.cs` | **Render** | **N/A (client-render)** | Not served; `POST` custom-data path → client render of uploaded data. |
| `GET /api/jumpmap?sector=&hex=&jump=` • `POST` • `…/jump/{jump}/image` | Render jump map → image | `JumpMapHandler.cs` | **Render** | **N/A (client-render)** | Not served; its **data** sibling `/api/jumpworlds` IS a gap. |
| Main page `?scale=&options=&style=&sector=&hex=&q=&marker_*=&yah_*=&ew=…` | HTML app + query params/markers/Empress Wave | (app) | HTML/UI | **N/A** | Frontend (Leptos) concern; URL-param compat is a client matter. |
| `/go/{sector}[/{hex}]` • IFRAME embed (`forceui=1`, postMessage) | Short nav links / embed | (app) | HTML/UI | **N/A** | Frontend routing/embed concern. |

## Implementation status (2026-06-14)

**Done — `crates/backend/src/compat.rs`** (the "cheap" reshapes; pure additions, no collision with the private contract): `/api/coordinates`, `/api/milieux`, `/t5ss/allegiances`, `/t5ss/sophonts`. Each verified byte-for-byte against the live API (modulo the live server's non-functional `\/` slash-escaping), supports `&jsonp=<cb>`, and is guarded by unit tests (`compat::tests`). New pure math in `tmap_core::astrometrics`: `location_to_coordinates` / `coordinates_to_location`. **Common deferrals across all four:** XML output (`Accept: text/xml`) and the semantic `/data/{sector}/...` URL aliases — see the cross-cutting section.

**Done — `/api/sector` converged onto the shared metadata parser (Phase B).** `build_sector_bytes` now parses each metadata source **once** via `parse_sector_metadata` and projects to the render `SectorData` (`render_border`/`render_route`/`render_label`/`render_subsectors`), replacing the previous ~8 `sector_*` calls per source that each re-parsed the XML — **16 → 2 `roxmltree` parses** per sector build. Verified **byte-identical** output across all 190 M1105 sectors (hash baseline → refactor → diff = 190/190), so the live map renders unchanged. `MetaBorder` now stores raw fields (a custom `Serialize` computes the public `LabelPosition`/`Path`); a `seq` index preserves `<Border>`/`<Region>` document order for the render layer.

**Done — `/api/universe` unified to the public shape.** Decision taken: **adopt the public PascalCase shape as the single contract** rather than keep a parallel private one. `get_universe` now emits `{"Sectors":[{X,Y,Milieu,Abbreviation,Tags,Names:[{Text,Lang?}]}]}` and our Leptos client deserializes that same shape (`UniverseResult`/`UniverseSector`/`SectorName` in `tmap_core::dto`; the private snake_case `Universe` is now in-memory only). Per-sector output is byte-identical to live. The index parse now retains all `<Name>`s (with `Lang`) and the `<Sector Tags>` attribute. **Why this works and isn't a parallel-path hack:** the public shape is a superset of what the client needs, so one URL serves both audiences. Our genuinely-private streaming endpoints (`/api/sector/{…}?lod=`, `/api/overlays`) have **no** public equivalent (the public API serves rendered tiles + per-format text; our browser renderer needs vector data) — they coexist as additive extensions and don't collide with any documented URL.

**Next — the "others":** `/api/search` envelope-unify is best folded into the planned Tantivy query-language work (one pass over search internals). The rest need new index/writer work: `/api/sec` text via `SectorWriter`; `/api/metadata` + `/api/credits` via a fuller metadata parse; `/api/jumpworlds` + `/api/route` via a cross-sector spatial index (`route` partly exists); `/api/msec` via `MSECWriter`. Plus the universe **completeness** gap (positioned-but-dataless sectors + `requireData`/`tag`/`era` filters).

## Compatibility test suite (TDD) — `crates/backend/src/compat_suite.rs`

An end-to-end suite drives the **real axum router in-process** (`tower::oneshot`) and checks every documented data endpoint against **golden fixtures captured from the live travellermap.com API** (`crates/backend/tests/refs/*`). JSON is compared as `serde_json::Value` (order- and `\/`-escaping-insensitive); the T5SS code tables are compared as a **set keyed by `Code`** (their array order is incidental — the reference uses .NET culture-sensitive collation). This is the parity oracle for the whole effort.

### Live parity (2026-06-16) — golden + live double-check

Goldens are snapshots and silently rot. So each data-endpoint test **also** sends the *same* request to live travellermap.com and our in-process router and compares them — failing if (1) we call the reference wrong (status mismatch) or (2) the bodies differ (beyond documented equivalences: `\/`-escaping, key order, T5SS sort, the metadata route-notation normalization). **On by default** so a local `cargo test` checks live; **CI sets `TMAP_SKIP_PARITY=1`** to skip it (network-dependent → slow/rate-limited/offline-hostile, doesn't belong in the fast gate). Helpers: `fetch_live` / `parity_json` / `parity_json_with` / `parity_text` in `compat_suite.rs`.

**TLS gotcha (2026-06-16):** parity fetches were silently failing with `Connection reset by peer` — *not* a network sandbox (an earlier wrong assumption), but because the reference host (old Windows/IIS/**Schannel**) rejects TLS 1.3 and even rustls's TLS 1.2 ClientHello with a RST. `curl`/SecureTransport and OpenSSL negotiate TLS 1.2 and succeed. Fix: the `reqwest` dev-dep uses **`native-tls-vendored`**, not `rustls-tls` (SecureTransport on macOS / bundled OpenSSL on Linux). Caveat: the suite fires ~15 concurrent live requests and the old server occasionally RSTs one (no retry), so a rare flaky failure on a parity test re-passes on retry; CI is unaffected (`TMAP_SKIP_PARITY=1`).

**Findings (verified 2026-06-16, ours vs live):** byte-identical for `/api/coordinates` (all forms), `/api/milieux`, `/t5ss/{allegiances,sophonts}`, `/api/credits`, `/api/sec` (TabDelimited + SecondSurvey), `/data/{sector}/{hex}`, `/api/jumpworlds`, **and `/api/universe` (full set, 1021/1021, every field including `Abbreviation`)**. The universe initially showed 47 `Abbreviation`-only diffs + 2 stray sectors; all three were **our bugs**, now fixed (see below), so universe parity is **strict** (no field excluded). NB: the apparent "drift" was *not* live running ahead — it was missing logic on our side.

**Universe parity bugs found + fixed (2026-06-16):**
1. **Abbreviation collision-dedup (47 sectors).** Synthesized sector abbreviations weren't de-duplicated, so e.g. `Far Shore`/`Far Shore 2` both became `Fars` and all eight `RC+…` sectors became `Rcxx`. Ported the reference `SectorMap.MilieuMap` dedup (`resolve_abbreviations`): process sectors in merge order, register names + abbreviations in a shared case-insensitive map, collisions fall back to `Far2`/`Far3`/… Now matches live's `Far2`/`Rcx2`…
2. **Empty-`<Name>` sectors dropped (Calidan).** `parse_milieu_index` did `names.first()?` — dropping any sector whose inline `<Name>` is empty *before* its `MetadataFile` name could be merged. Faraway's Calidan (empty inline name → name from `Calidan.xml`) vanished. Now kept (X/Y still required); name filled by the merge.
3. **Loose-file duplicate (Kruse).** The dir-fallback added a loose `Kruse.xml` (at `(0,-9)`) as a *second* Kruse alongside the authoritative `M1105.xml` entry (`(0,9)`) — position-only dedup missed it. Now the dir-fallback also skips sectors already loaded **by name** (the reference reads only metafiles).

**TDD convention:** endpoints not yet implemented (or not yet in the public shape) have their test `#[ignore = "…"]`d **with the target assertion already written**, so implementing one = deleting its `ignore`. Scoreboard:

```
cargo test -p tmap-backend compat_suite                       # active (must pass)
cargo test -p tmap-backend compat_suite -- --include-ignored  # full target (red until done)
cargo test -p tmap-backend compat_suite -- --ignored --list   # what's left
```

Current (2026-06-17): **89 tests, 0 ignored** (was "2 ignored red targets" — both gone). Beyond the prior set: search envelope + **11 query-language tests** (behavioral, our index) + **12 per-query live-parity tests** (`parity_search_*`, set-equality vs live, cap-guarded); `msec_text`; **JSONP + XML** tests for universe/search/credits/jumpworlds/metadata/route; **legacy SEC** (`sec_legacy`) + `sec_default_is_second_survey`; **route** Subsector + **full coord parity**; `metadata_xml` (per-collection counts vs live); `/data` subsector/quadrant aliases (×8) + single-world; **POST** sec round-trip + metadata XML→JSON convert/reject.

XML content negotiation: `Accept: …xml` (or `accept=text/xml`) serves **all** JSON data endpoints — coordinates, universe, search (`<results>`), credits (`<Data>`), jumpworlds (`<JumpWorlds>`), route (`<ArrayOfRouteStop>`), and metadata (`<Sector>`, `SectorMetadata::to_xml`) — via `compat::wants_xml` + `respond_negotiated` (jsonp wins, then XML, else JSON). SEC/MSEC are text formats (no XML). JSONP (`&jsonp=cb`) is supported on every JSON endpoint. (Live wraps roots with `xmlns:xsi/xsd` + pretty-prints; tests compare structure/counts, not bytes.)

Semantic `/data/{sector}/...` URLs (port of the `Global.asax.cs` table) registered: `/data/{sector}` & `/sec` (SecondSurvey), `/tab`, `/coordinates`, `/credits`, `/metadata`, `/msec`; the region aliases `/data/{sector}/subsector/{A–P|name}` and `/data/{sector}/quadrant/{alpha|beta|gamma|delta}` (+ their `/sec`,`/tab`); the single-world `/data/{sector}/{hex}`; and `/data/{sector}/{hex}/{coordinates|credits|jump/{jump}}`. Only `/image` (render → N/A) is absent.

Real gaps/bugs surfaced and fixed while building these tests: (1) `/t5ss/allegiances` was missing the 16 stock allegiances the reference merges on top of the `.tab` (M1120 splinter states + cultural regions; no `Location`); (2) `sector_credits` double-counted text (iterating every descendant and calling `.text()`), duplicating the `# Credits:` line; (3) sectors with no declared abbreviation now synthesize one (`Sector.SynthesizeAbbreviation`, e.g. `Zhdant`→`Zhda`) in both `/api/universe` and `/api/sec`; (4) world serialization order must be `(subsector, col, row)` — the reference enumerates a `WorldCollection` column-major, not file order. (Note: a few worlds differ from the *live* server purely because its `res/` is ahead of this checkout — data drift, not a writer bug; our output is faithful to local `res/`.)

## Our backend extensions (no reference equivalent — keep)
- **`GET /api/overlays`** — bundles macro borders/routes/rifts (`res/Vectors/`) + macro world labels (`res/labels/Worlds.xml`) into one JSON payload. New, well-motivated for client-side rendering; not part of the public API.
- **`GET /api/sector/{milieu}/{name}?lod=full|overview`** — combined worlds+borders+routes+subsectors JSON with LOD projection. Core streaming primitive of the rewrite; closest ref split is `/api/sec` (worlds) + `/api/metadata` (metadata).
- **`GET /api/res/{*path}`** — static passthrough of `res/` assets. Reference serves these as plain static files; documented marker URLs are root-relative `res/markers/...`, so the frontend (or a static mount) must also serve them at `/res/...` for documented marker URLs to resolve.
- **`POST /api/admin/flush`** — dev cache-flush; loosely mirrors ref `/admin/flush` (admin-key gated there).

## Remaining gaps (2026-06-16)

Every documented **data** endpoint is implemented + live-parity-tested. What's left is render-only or niche:

- **Render endpoints — `/api/tile`, `/api/poster`, `/api/jumpmap` (+ `/data/.../image`).** Intentionally **N/A**: this rewrite renders client-side (Leptos/WASM). Not a compat gap — a design decision.
- **Search specials (niche).** `(random world)` and the canned `(name)` → `res/search/{name}.json` published-route/tour lookups (`SearchHandler` `SPECIAL_REGEXP`) are not handled.
- ~~**Search result *ranking* (`Importance`).**~~ **Done (2026-06-17).** Results match the reference order exactly — `{Ix}` importance descending, ties broken by each kind's SQL `SELECT DISTINCT` coordinate order (`coord_key` in `search.rs`). The 12 `parity_search_*` tests now assert an **ordered** diff vs live (cap-guarded) and pass.
- **POST breadth.** `POST /api/sec` (reformat) and `POST /api/metadata` (XML→JSON) cover the central convert path; the reference also accepts MSEC-format metadata input and richer lint diagnostics — not ported.
- **Cosmetic live diffs (documented equivalences, normalized in tests, not bugs):** metadata/MSEC allegiance ordering; the `Route.FixHex` start/end hex notation; named-color casing (`olivedrab` vs `OliveDrab`); MSEC within-allegiance line order (.NET unstable sort).
- **Test coverage worth adding (not gaps, just thinner spots):** most parity tests pin **Spinward Marches** — a second sector (different milieu / cross-sector) for sec/metadata/credits would catch sector-specific regressions; the `/data` alias XML/JSONP variants and the extension endpoints (`/api/sector?lod=`, `/api/overlays`) have shape tests but no live oracle (none exists — they're ours).

## Prioritized data-API gaps — ✅ ALL DONE (historical, kept for context)
1. **Coordinates API** — highest value-to-effort; math already in `tmap_core::astrometrics`. Parse `sector[&hex|&subsector]`/`sx/sy[&hx/hy]`/`x/y` → `{"sx","sy","hx","hy","x","y"}` (+XML/JSONP). `CoordinatesHandler.cs`.
2. **Search envelope + query language** — keep the route, emit the documented `Results.Items[].{World|Sector|Subsector|Label}` shape; add wildcard/prefix query language + subsector/label hits. Natural home for the planned Tantivy upgrade. `SearchHandler.cs` + `server/search/`.
3. **Universe alignment** — reshape to `{"Sectors":[{"X","Y","Milieu","Abbreviation","Tags","Name":[…]}]}`, add `/data` alias + `requireData`/`tag`/`era` filters + XML/JSONP. `UniverseHandler.cs`.
4. **SEC text output + `/data/{sector}[/sec|/tab]`** — emit `type=SecondSurvey|TabDelimited|Legacy` text + subsector/quadrant slicing + toggles. Need the **writers** (`SectorWriter`). `SECHandler.cs`.
5. **JumpWorlds API** — cross-sector gather + hex-distance filter → `{"Worlds":[…]}` PascalCase. `JumpWorldsHandler.cs`.
6. **Metadata (standalone)** — full sector metadata JSON/XML. `SectorMetaDataHandler.cs`/`SectorMetadataSerializer`.
7. **Lower:** `/api/milieux`, `/t5ss/allegiances`, `/t5ss/sophonts` (trivial table dumps); **Credits** (needs metadata credit fields); **Route** (constrained pathfinder — most effort, shared with the jump-route planner); **MSEC** (legacy, lowest).

## Cross-cutting compatibility requirements (all data endpoints)
- **PascalCase JSON envelopes** matching the reference `Results.*` classes (our snake_case DTOs won't deserialize in existing clients).
- **Content negotiation:** honor `Accept: text/xml` / `accept=text/xml` (reference default is actually XML; JSON is the common opt-in).
- **JSONP:** support `&jsonp=callback` on Coordinates/Credits/JumpWorlds/Search/Universe/Metadata/SEC/MSEC.
- **Semantic `/data/...` URL family** as aliases for `/api/...` verbs (sector by name OR T5SS abbreviation, e.g. `spin`).
- **Sector identification flexibility:** every sector-addressed endpoint accepts name, T5SS abbreviation, or `sx/sy` — our backend currently takes only exact sector name in the path.

---

## Compat-layer efficiency evaluation (2026-06-12)

Deciding factor for **thin compatibility layer vs. ground-up**: how efficiently each documented data endpoint can be served as a wrapper over our *current* in-memory model (universe index, lazily-built per-`(milieu,name,lod)` `SectorData`, per-milieu name search index, parse-once macro overlays, raw `res/`, `tmap_core::astrometrics`). Render endpoints (Tile/Poster/JumpMap) excluded.

| Endpoint | Compat cost | Why (work forced on our current model) | Recommendation |
|---|---|---|---|
| **Coordinates** | **Cheap** | All param combos resolve via the cached universe index + pure `astrometrics` math. No scan, no new structure. Just reshape to `{sx,sy,hx,hy,x,y}` + XML/JSONP. | **Compat-layer.** |
| **Universe** | **Cheap** | Full per-milieu index already in memory; reshape envelope. Caveat: `Tags` + localized `Names` aren't retained in `SectorIndexEntry` — thread them through (small change). Filters are cheap post-filters. | **Compat-layer** (+ retain Tags/Names). |
| **Milieux** | **Cheap** | Enumerate `milieu.tab` (tiny, once). Pure reshape. | **Compat-layer.** |
| **T5SS allegiances/sophonts** | **Cheap** | Two small static tables, cached, reshape to `[{Code,Name}]`. | **Compat-layer.** |
| **SEC/tab/SecondSurvey text** | **Moderate** | Data already parsed to full-fidelity `World`; cost is **new code not new data** — a faithful `SectorWriter` (dynamic widths, header/metadata blocks, `sscoords`), subsector/quadrant filtering, format-sniffing `POST`. Tractable per-request. | **Compat-layer + port `SectorWriter`.** |
| **Metadata** | **Moderate** | We hold only a **subset** (subsectors/borders+region/routes/border-colors). Documented doc also carries allegiances, labels, credits, `<Settings>`, full `<Stylesheet>`. Needs a fuller metadata parse + XML/JSON serializer, not just reshaping `SectorData`. Per-sector, cacheable. | **Mostly ground-up parse/serialize** (or accept a reduced doc). |
| **Credits** | **Moderate** | World lookup cheap; but credit fields (Author/Source/Publisher/Copyright) come from metadata we **don't parse** today. Shares the metadata-credit work above. | **Compat-layer once metadata credits parsed.** |
| **Search query-language** | **Expensive-as-compat** | Our index is a flat lowercased **name** list scored by substring — no attribute fields, no subsectors/labels, no wildcard/phonetic, no field-scoping. Bolting on `exact:`/`uwp:`/`alleg:`/`in:`/wildcards means re-scanning all worlds per query = rebuilding the index in disguise. | **Ground-up: the planned Tantivy inverted index** (name + UWP/PBG/Zone/Allegiance/Stellar/Remarks + subsectors/labels). Envelope is cheap; the matcher isn't. |
| **JumpWorlds** | **Expensive-as-compat** | Worlds within N parsecs straddle sectors; no cross-sector spatial index, so each call loads+scans up to ~9 neighbor `SectorData` and hex-filters. Heavy/repetitive. | **Ground-up (light): a per-milieu spatial/world index** (absolute-hex → world) so neighborhood gather is a bounded lookup. |
| **Route** | **Expensive-as-compat** | Shortest constrained path over a world graph that doesn't exist; a shim builds adjacency across many sectors **per call**, plus refuel/Imperial/red-zone predicates. | **Ground-up: same spatial index as JumpWorlds + A*/Dijkstra** with constraint predicates (shares the jump-route planner engine). |
| **MSEC** | **Moderate** | Legacy sec2pdf text; needs a ported `MSECWriter`. Low value. | **Compat-layer + `MSECWriter` (defer, lowest priority).** |

**Verdict.** *Thin compat layer, do first (pure reshape of data we already hold):* **Coordinates, Universe, Milieux, T5SS code tables** — Coordinates is the top value-to-effort win. *Compat layer but gated on a writer:* **SEC/tab text** (`SectorWriter`), **MSEC** (`MSECWriter`). *Gated on a fuller metadata parse (one shared piece of new work):* **Metadata + Credits**. *Justify ground-up (a shim would re-scan/rebuild per call):* **Search** → Tantivy inverted index; **JumpWorlds + Route** → one shared cross-sector spatial/world index (+ pathfinder for Route). For the ground-up trio the JSON envelope is a cheap final wrap; the cost is entirely the index/graph we don't have today.
